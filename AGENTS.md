# quantam-fs — agent entrypoint (all vendors)

Local-first, serverless, post-quantum-encrypted, real-time multiplayer file system. Hackathon build.

Do this now, in order:
1. Read `docs/agents/PROTOCOL.md` in full (~80 lines). It is the only rules file and it is binding.
2. `ls docs/tasks/active/` — if a task's `prompt:` matches yours, resume it (PROTOCOL §3). Otherwise start fresh.
3. Stay inside your area: `client/` (Aaryaman) or `backend/` (teammate). Other files changing = another live session; ignore them.

Layout: `client/` GUI · `backend/` protocol core · `docs/` VISION.md, agents/, tasks/, decisions/
Read `docs/VISION.md`, decision bodies, or done tasks only when PROTOCOL §0 says to.
