# Client: home redesign (GT Walsheim, blue-black theme, server headings + vault cards, modals)
area: client      status: done      opened: 2026-09-12      by: Aaryaman
prompt: >
  [follow-up, same session] Also i would like if the FINAL step of the animation (like literally RIGHT before it goes into the static mode) was SLIGHTLY slower and eased into the static lines as it is just SLIGHTLY abrupt as of now. It is absolutely beautiful otherwise, just that tiny last bit of the animation. I was also wondering (this isnt super important) but it would be very cool if we could have a SUPER SLOW moving gradient running through the gradient lines thng after they become currently static - like just a little tiny bit of ambient movement so there is something going on. Make sure this doesnt come at the expense of the beautiful animation we have already though

  [follow-up 3] also clicking the plus button should let me actually do something for a server - open up a new modal where i can put in the name of a new vault and allocate memory based on how much is on the server (it should be some sort of dynamic slider based on how much the other vaults are already consuming vs how much is available to provision). just make sure it is clean, intuitive, minimal, and not sloppy.

  [follow-up 2] make the quantumfs one knotch less bold in font weight. as i type in the fields for the join code it should automatically jump to the next field obviously. The heading for the modal should also be one knotch down. In fact, NEVER ever reach that heading weight, the max heading weight will always be one knotch down from that.

  couple of design pet peeves - the final left color strips can take up like 45% of the width. you've spelled quantum wrong - it is QuantumFS. Remove the Iconmark there, its fine. Make it bigger, and increase the font weight for the wordmark. Also that whole content section can have a slightly wider width. Also i do not want to use urbanist anymore - instead let us use "GT Walsheim Trial" (it is on my computer installed, please update the weights accordingly. The 'server n' heading should have a slightly bolder weight, the QuantumFS heading should have an even more slightly bolder weight. Also, the design of the cards / buttons isnt very nice right now. I would prefer if each server card wasnt so much of a card, Instead we can isolate the server heading and plus icon from the card, having it rest as a heading right above a card filled with all the vaults underneath it. Make the server icon a bit smaller too to match the size of the  server name text. Remove the weird gradient line and insane indentation underneath the server heading. I do not like the icon being used for a vault. Also give the vaults names instead of just vault 1 vault 2 etc. By the way, feature the IP address of each server next to its title in some super clean, muted fashion. Underneath each vault name, feature the number of members within it as well. Upon clicking "Setup New Server", i want to see a very clean modal pop up which prompts me to enter the IP address of the server, and upon entering it and clicking the submission button i will see a nice confirmation that the server was just added.  Actually, I would prefer if the "Setup New Server" button with the icon next to it was not an actual button like it is now, but rather a very clean label-button / link style-button (without any underline) in the extreme bottom-right of the app, so its kinda small but its intuitive enough. The "Join a vault" button will then just go underneath the list of server vaults and the 'OR' divider will dissapear. Upon clicking this button, a modal will pop up which will have the heading "Join a Vault" and the muted description "Enter the join code" which will then render 6 square input fields (like a confirmation/verification code input looks like) to input this join code. And then a white full-width button underneath to indicate "Join". BTW the join server modal will be similar (but the field will just be one field for the IP address. ALso ensure you indicate the MB/GB used next to the number of members in a vault, seperating the two in muted text with a little dot in the middle. Finally, I am kind of unhappy with how black the background black is, I think if we just SLIGHTLY make it a lighter shade, towards a blueish-gray tone, it could look much nicer. Something closer to #010513 would be good (also ensure you adjust the secondary, tertiary background shades accordingly.

## Plan
- [x] A theme/brand: GT Walsheim Trial via local() @font-face (Lt 300, Rg 400, Md 500, Bd 700, Bl 900), Urbanist removed; bg #010513 + derived surfaces; QuantumFS everywhere (APP_NAME, title, productName, identifier); panel 45%; shader/fallback black = page bg
- [x] B data: Vault.usedBytes + real vault names in TS seed, mock, Rust bridge
- [x] C home: wordmark 34/700 no iconmark; 460px box; server heading (icon 16 + name 500 + muted IP + plus) above a vault card (name 500 + "N members · 1.2 GB"); Join a Vault row under the list; fixed bottom-right "Setup New Server" link-button; Modal + code input + Setup Server / Join Vault modals with confirmation states
- [x] D splash tail: slower, softer ease into the settled state (settle 1250→~1550 ms, gentler out-tail; blend reaches 1 only at s=1); after settling, a super-slow ambient drift along the gradient (≤4 % luminance, ≤0.015 uv boundary shift, ~30 s period, fades in over ~2 s), barrage untouched
- [x] F data: server.capacityBytes, vault.quotaBytes, createVault(serverId, name, quotaBytes) in TS + Rust
- [x] G plus button → "New Vault" modal: name field + allocation slider (consumed vs available on that server), confirmation, list refresh
- [x] E verify screenshots (home, modals, small viewport), relaunch desktop app, commit

## Checkpoint   (overwrite in place · ≤10 lines · write BEFORE starting the next step)
done:       all steps; see Outcome
in-flight:  none
next:       none — task complete
open:       none

## Outcome   (fill at completion · ≤10 lines · facts only)
changed:    client/src/styles/globals.css (GT Walsheim Trial via local(), #010513 theme, surface tokens), brand/index.html/tauri.conf.json/package.json/README (QuantumFS), splash (panel 0.45, BLACK = page bg, settle 1250→2800 ms with cubicBezier(0.6,0,0.12,1), u_idle uniform + ambient band, App passes idle), lib/backend + src-tauri/src/bridge.rs (vault names, usedBytes, quotaBytes, capacityBytes, createVault input, 6-char join codes), features/home (wordmark 34/500, server headings + vault cards, corner Setup New Server link, Join a Vault row, Setup/Join/New Vault modals), components/ui (Modal, PrimaryButton, TextField, CodeInput, ConfirmState, AllocationSlider), lib/format.ts; Urbanist files removed; docs/decisions/client-typography.md
verified:   npm run typecheck → 0 · npm run build → ✓ built · cargo build → 0 warnings · flow scripts (headless Chrome): setup-server add → confirmation → list shows "Server 3 · 10.0.0.42:7447"; join code auto-advance A,B,1,2,C,D → confirmation; new vault "Marketing · 27.5 GB" → row "1 member · 0 B"; reopen shows a fresh form on the first frame (control run without the fix failed) · splash: barrage frames byte-identical, tail deltas ≤ 0.03/level, idle deltas 0.5–1.6, coral endpoint #FA7A7A, black point 538–547 px of 1280 · fonts.check("500 15px GT Walsheim Trial") true; body bg rgb(1,5,19)
not-done:   real daemon integration (bridge still an in-memory mock on both sides); the slider's 120 ms CSS transitions ignore prefers-reduced-motion; "This server is full" state still shows "0 B" as the header value
gotchas:    max font weight is 500 everywhere (decision client-typography) · fonts come from the OS via local(); machines without GT Walsheim fall back to system-ui · Rust crate stays `quantamfs`; bundle identifier is now fs.quantum.app (fresh app state on macOS) · modals reset state during render on open (effect-based reset flashes the previous confirmation)
