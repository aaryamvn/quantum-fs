# Vault workspace: Finder-like file browser after the splash/home screen
area: client      status: done      opened: 2026-09-12      by: Aaryaman
prompt: >
  FOR THE TAURI CLIENT: Now we have to build out the actual file system interface in rust—what happens AFTER the splash screen interface / onboarding experience. Let me emphasize how beautiful, minimal, and thoughtfully designed this needs to be. The icons for folders and files need to be carefully designed or sourced, I would especially appreciate a 3D version of each possible icon. The experience needs to be as snappy and intuitive as the finder experiecne on macos. Double clicking folder/file name must support editing-in-place. Right clicking such an item must reveal a dropdown menu filled with options as-to what can be done (view info about the file - we must be able to support this, rename, delete, duplicate, copy, share, change the color of the folder. Some of these can be featured as nice beautiful centered icons at the top of the right-click popover, underneath which is a seperator/divider line, underneath which is the rest of the options. I wouild also like an "History" option which shows the history and evolution of the folder. The owner/creator should also be featured someplace nice in this right-click dropdown with their profile image (just their initials i suppose within a colored circle) as well as time and date of creation. There should also be a function to control access rights of a folder/file (ONLY possible if you yourself are the creator - this lets you granularly set permissions for who gets to edit a folder (either on the right click or for the current folder you are in - using an action bar at the top of a open folder). there will be a minimal row of action icons+labels (this composes each action button, designed similarly to the "Setup new Server" button, at the TOP of each folder as it is opened. So pretty much on every screen this will exist at the top of the folder. Some of the actions include modifying access permissions to the specific folder of individuals who have access to the vault (the creator of the vault will be an admin, and can set users to viewers, editors on a per folder basis).
  We do not have the concept of a home page or anything really. We will have ONE sidebar on the left - this will compose a tree of servers and vaults undernath the servers. At the bottom of this sidebar will rest the users profile photo and name. At the extreme top and center of the sidebar we will render the iconmark svg from a while ago, however we will render it slightly bigger so that it is visible (not HUGe but like relatively big compared to most iconmarks). At the bottom right of any screen should also be a prominent plus button with our signature gradient in the background, and this plus button will be enclosed in a circle. Upon clicking the button will beautifully, kind of gooey-animate-out a popout menu of things that can be created- a folder or a file. Clicking this will, of course, do exactly that and create a new file or folder in the current directory with the name field active so user can type-in-place the name of the file/folder. In the center pane of the app i.e. what is revealed when a vault is selected will just be the root directory of the vault and a 'grid'/'canvas' of icons of all the folders and files within it - basically like how finder is layouted.
  there must also always be revealed a right sidebar which will show the content and basic metadata/information of any currently 'selected' folder or file (selected just means it is single clicked-on, or the arrow keys selected it, not that it is clicked INTO). it should be clean and well designed and organized.
  another important feature is that, since stuff wont always be immediately available on the users computer, it should be indicated with a little icon next to the folder/file name (like how icloud does it in finder) whethr it is ready to open or not. folders will always be ready to open since the actual file tree will always be available on every computer, however actual files may not always be ready.
  Upon hovering on the vault in the left sidebar will reveal a small settings cog on the right side, upon clicking that will bring up a large settings modal which has sidebars to configure the vault. We will have a general tab for some basic settings like name, server it belongs to etc. And then we will also have a members tab to control who are the members, if they are admins or members(and changing it as such), and a way to share the join code to the vault as well as rotate it. Any other pertinent setting related informatioin you can see from our vision files can also be included in this modal. For collaborators you should also be able to see who is currently online or not.
  The top bar of the app is very important - this will be a skinny, lightweight bar but will carry a lot of power. We will have back/forth carets, very good breadcrumbs on the left side of the bar. on the right side, we will have a nice and clean view of all the collaborators currently online/active within the vault - similar to google docs (but with much smaller profile pics). hovering on these must reveal a contact card like popover which reveals the name and profile pic and last edited file/folder of the user. and we can truncate it at x number of users by showing something like (+y) in the final circle, which, when hovered, will reveal a list of all the other online members. it is also imperative there exist a very powerful search function, so a search bar must also be included somehow in this header bar. clicking this will open up a search modal which is incredibly sophisticated and can traverse all files no matter what their position or nesting level is within the app.
  ensure you create icons for all sorts of file types as well - word, excel, powerpoint, html files, zip files, md files, python files, typescript files, other language files, and pretty much anything else you can think of - map like maybe 200 extensions to high quality, preferably 3d, beautifully designed icons.
  by the way this whole interface is revealed once a vault is clicked into from the splash screen. i would appreciate some novel, unique, clean and non-tacky but super cool and beautiful animation that is ideally full-screen and expands into the vault somehow once it is clicked on.
  OH - and one EXTREMELY nice use case I want to deal with. Have a beautifully integrated CHAT BAR at the center and bottom of every single centerpane. This will be used to initiate agentic conversations about any files in the vault, which will integrate with the users local claude or codex subscription. We can work on the actual implementation of this later-on, but ensure the chatbar exists and is beautifully designed. Placeholder should be "Ask about your files"
  Another thing - a very brief section I would like in the left-sidebar at the top is a "Recents" section where certain files/folders (upto n) are briefly LISTED (with the same icon that their grid icon in the canvas uses placed to the left), and to the right of their tab is a muted writing of their vault associated.
  Some very important animations for dealing with folders and files:
  Local drag. Pointer capture, not HTML5 drag. Pickup: scale 1.06, 16px contact shadow, 1.5° tilt toward velocity, 80ms. Flight: Motion spring stiffness ~380, damping ~32, mass 0.9 — not linear, not bounce. Tiles in the drop target open a gap with layout animation. Drop: squash Y 0.96 → 1.0 in 180ms. Drop into a folder: folder pulses violet 0–0.35 opacity, tile sucks in (scale 0.2 + fade, 220ms), folder badge count ticks. Esc/cancel springs back along the same path. While flying, transform/opacity only — no layout thrash.
  Multi-select. Marquee + Shift-click. A stack fans 4px; top tile follows the pointer; others trail 40ms apart, max 5 visible.
  Cursors. 5px dot + 8px triangular pointer in peer color, 8px gaussian glow. Label: 11px medium on a 20px pill, 8px behind the tip. Interpolate 80ms exponential — never teleport. Two peers in one folder: both cursors visible, hovered files lift 1px. Offline: avatar dims, "7 queued" chip. (modify this to whatever you think is appropriate based on our proportions)
  Remote move. Cursor arrives first; 120ms later the tile levitates and follows a cubic path. Motion smear: duplicate tile at 20% opacity, blur 12px, 180ms.
  Peers. Aaryaman Maheshwari #FF7B7B, Justin #4E0EFF, Maya #7BFFD6, Kenji #FFC27B. Avatars: two-stop coral/violet/black gradients from peerId, initials, no photos.
  Loop (40s, elapsed-ms, not nested timeouts).
  0s Maya cursor enters from right, hovers Film
  4s Aaryaman picks up Brand, flies it into Projects (full pickup/flight/suck)
  11s Justin creates "Launch" (tile pops from 0.8, violet flash)
  16s Kenji drags a three-file stack into Launch
  24s Maya and Aaryaman meet over poster.hdr; Maya yields; Aaryaman double-click pulse (no editor)
  32s Justin idles; Archive receives Brand back
  40s crossfade reset, loop
  Note: later i will delete all of this and link it up with the actual backend so build it in that way such that it looks great right now but i can scrap it later (yet all of the functions will still work once i integrate the 'backend')
  Use dynamic workflows and send out a few hundred agents to get this built out perfectly and exceptionally well. No mistakes can be made here.


## Follow-up request (2026-09-12, verbatim)
prompt2: >
  ok fully remove the cursors and live dragging feature. if it happens itll just automatically reflect and ill build a polling function for that later. remove the Online - Peer_aary thing from where the name and pfp is just keep it name and pfp. Use only Inter for body text and keep the gt walsheim for exclusively headings. also ensure that the server names are a bit bolder in the sidebar. For online servers use a white dot not green. If i double click a folder i should actually go into the folder - that doesnt happen right now. For any profile photos displayed anywhere, do not use a colored border - always use a white border. also the initials are not always perfectly centered within these profile photos. Do not indicate how many actions are queued for any member. also make sure every profile photo always has a subtle white border and never a colored one - this goes anywhere a pfp is displayed. also server names/headings should be white and not muted. Remove the plus button in the bottom-right entirely. Remove the tag indicating how many files are in each folder on top of its icon. If I, as a user, drag something into the folder - it should actually go into the folder.  If i use my arrow keys and move between files after initially clicking on a file, it leaves an extra outline on the first file/folder - remove that outline. Remove the deletion tooltips entirely. Remove the vertical bar to the left of an active vault in the sidebar which indicates that it is active - the highlight in the background is enough. Ensure all profile photos will always be the same background gradient of the purple one - do not have any other color. the +n circle in the pfp row needs to be on top it is currently on bottom. The label of a folder might need to be just slightly beneath it because the active highlight is too close to the folder/file icon.The cancel button (and that entire variant of button) needs to be displayed with a muted background color / secondary / tertiary background rather than having no background. Also i take back the white border thing - we need a border ALWAYS but it should be a violet-ish color rather than white to match the inner color. Make sure this color is very appropriate and makes the entire thing a little less dense visually speaking. Use american english always so no "colour" Some of these mini headings like RECENTS, SERVERS, and the likes need to be formatted differently - Never have this entire class of heading be all caps, make the font size slightly bigger, and write it with just a capital letter instead of all caps. So "Recents", "Servers" "Access" "Color" etc. Also, remove the iconmark we have right now in the left sidebar. Instead, replace it with the same wordmark we have on the splash screen and left-align it in the sidebar. In the AI chat bar in the bottom, remove the icon to the left and the tag indicating the selected folder completely. Remove the claude/codex dropdown as well. Give the send button our signature gradient background ideally with a bit of blur and a moving gradient so it looks kind of cool. Also remove the caret ENTIRELY from the server tabs on the left. Where the caret icon is currently should be where the server icon is. ONLY when the tab is hovered upon, should we replace the server icon with the caret dropdown icon and when hovered out of, it goes away. Also the right sidebar is just slightly dense and not very breathable so if you can make it slightly less dense if possible? dont remove too much essential stuff though just make it mildly more breathable.

prompt3: >
  There will never be an explicit download button, just double clicking the file/folder will downlaod it if not already downloaded (and will indicate that it is doing so very minimally somewhere)

## Plan
- [x] 1 Contracts as code: backend types + BackendClient methods, workspace store, icon types (docs/decisions/client-workspace.md)
- [x] 2 Foundation: 3D icon system (registry ~220 exts + families), UI primitives (Avatar, Popover, Tooltip, Kbd, Toggle, Select, Toast), libs (peer colors, time, path, fuzzy search)
- [x] 3 Data: mock FS engine + seed tree, Rust bridge mirror + Tauri client, demo presence loop (src/lib/backend/demo/, deletable)
- [x] 4 Workspace UI: sidebar, top bar, breadcrumbs, presence, inspector, canvas/tiles/rename/keyboard, action bar, context menu, FAB, chat bar, modals (info/history/access/share/vault settings/search), drag, marquee, cursors, remote move, vault dive transition
- [x] 5 Integration: App routing home → dive → workspace, dev query params for screenshots, typecheck/build/cargo
- [x] 6 Review: spec reviews, adversarial verification, screenshot QA, fix loops
- [x] 7 Follow-up pass: remove peer cursors/remote-move flights/FAB/count badges/queued chips, Inter body + GT Walsheim headings, avatar gradient+violet border, sidebar wordmark/servers/caret-on-hover, dblclick + drag-to-folder reliability, captions sentence case, American English, chat bar simplification + gradient send, inspector breathing room
- [x] 7b No explicit download buttons; double-click downloads remote files with a minimal indicator (tile ring + inspector line, no toast)
- [x] 8 Re-gate + revised screenshot QA + Outcome

## Checkpoint   (overwrite in place · ≤10 lines · write BEFORE starting the next step)
done:       all steps; follow-up passes applied; gates green
in-flight:  none
next:       none — task complete (see Outcome)
open:       none

## Outcome   (fill at completion · ≤10 lines · facts only)
changed:    client/src/features/workspace/** (store, sidebar, topbar, inspector, canvas, actionbar, menus, chat, modals incl. settings+search, drag, keyboard, transition, WorkspaceScreen), client/src/components/{icons/**, ui/*, brand/Wordmark.tsx}, client/src/lib/{backend/{types,client,index,tauri,mock/,demo/,seed/}, search/, path, time, color, keys, layers, devQuery}, client/src/{App.tsx, app/appShell.ts, styles/globals.css, main.tsx}, client/src-tauri/src/{fs_types,fs_state,fs_commands,lib,bridge}.rs + tauri.conf.json, client/scripts/{shoot,seed-fs}.mjs, client/package.json (+zustand, @fontsource-variable/inter), client/README.md, docs/decisions/client-{workspace,typography-inter,copy-american-english}.md
verified:   cd client && npm run typecheck → clean · npm run build → built · cd src-tauri && cargo build → Finished · screenshot sweeps of root/selection/context menu/search/settings/history/access/rename/marquee/drag-drop/dive/demo states read and checked
not-done:   full adversarial QA pass was stopped at the human's request (usage); 171 deep-review findings (mostly minor; see the session transcript) were not all applied; Tauri window was not screenshotted (cargo builds, commands mirrored)
gotchas:    demo peers are a wrapper (src/lib/backend/demo, one call in createBackend) — delete the folder + that call to remove them; peer cursors/remote flights/FAB/count badges were removed on request; search runs client-side over listTree in both runtimes; fs.json is the single seed for TS and Rust (npm run seed:fs); dev switches ?vault=&folder=&select=&ui=&demo=off|<ms>
