# Demo bug-fix sweep, onboarding name screen, kick queue, reset + populate servers
area: client (cross-area: backend directory profile store, kick denial signalling, demo scripts)      status: active      opened: 2026-09-12      by: Aaryaman
prompt: >
  there is still some work that needs to be done to fix and squash some basic bugs in quantumfs. firstly i dont think our vault names are being displayed correctly? if i put in a name of a vault while I create it, it should def actually use that name of the vault. right now it sort of just displays "Vault" and thats it even if it has a name. Second, the server IP address should always be displayed in the sidebar and main page - there is no "Server 1", it MUST always be connected to the actual server running and the ip address should also always be displayed as such. also we are certain that the storage capacity of each vault is being accurately represented based on how much the sum of the total capacities allocated by each client add up to right? i would love if we can have a clean visualisation of how much each client contributes to the overall storage capacity which is represented by some sort of a circle pie chart (rings only, not really heavily filled) in the settings in a prominent and relevant location. also how do users exactly 'name' theirselves? if its their first time in a vault and their name isnt already associated with their computer/client then can we prompt them to sign up quickly by putting in their name? actually like if they download/launch the app for the first time can we replace all of the content we have right now with just a field to put in their name and click "next" and its heading is "Hey there, what is your name?" right underneath QuantumFS. After this it slides into the vault and server list and lets them proceed normally but their name is now always associated with whatever identifies their client/computer (and we store this on central server) and it stays forever whenever they open the app next. do this for all instances here-on out please i am sort of just resetting all of the data for the presentation. also this is super important - make sure that the delete member function is very well built out (only the owner/admin of a vault can do it) and that the queue function works so even if the other user is offline, as soon as they are back online they receive a signal from the central server that forces their instance to remove the vault from their directory and immediately indicate that this has been done in a toast, and then return them to the home page. This should happen in real-time. Also change the "Setup New Server" to "Add a Server". Also, idk why but the background of all input fields is horribly off contrast from the primary background, so ensure that this secondary background is a right shade of the primary one / a lighter shade. this is only for some input types like the boxes for the code. also can we make sure that we rotate the vault id or whatever each time we kick someone out (i assume this happens already but still). Now - I want you to reset all the servers and vaults, fully wiping them all out. You can have two virtual machines open with QFS open in them (and nothing else - no server or anything). Before that, you will run a central server and two orchestration servers on my machine. you MUST make sure that my laptop will remain caffeinated and these servers will NEVER die no matter what until i actually cancel out of them, because I am using this laptop and my friends laptop for the demo (he will connect to the server on my laptop). Populate each server with 3 vaults each, and each vault must have a good amount of files - like i would say 5-15 items per root directory in each vault, and then maybe the first folder can have a lot of sub files. Try and put a lot of variety in the types of files created e.g. word docs, python files, typescript, md, whatever else makes sense. also ensure it is easy for me to delete vaults. and also try and provision a decent bit of storage from each client connected. i assume btw that the storage of the vault goes up as new clients get added  right? also make sure that the terminals for the servers DO NOT error out ever and that they perfectly log everything and just dont fuck up. Also make sure that the "QuantumFS" on the splash screen uses the GT Walsheim font please. finally make sure the profile photos of the collaborators are stacked/layered well i think they are but the transparency is an issue so just find an actual shade for the offline muted variant and use that rather than decreasing opacity, and ensure that the left-most pfp always gets the top priority in the 'stack'.

## Plan
- [ ] Investigate every concern (read-only) and write per-unit specs.
- [ ] Client fixes: vault name pipeline, server label = IP:port, "Add a Server", input backgrounds, splash wordmark font, avatar stack.
- [ ] Onboarding name screen + client-wide display name persisted locally and on the directory (backend profile store).
- [ ] Kick: owner-only gating, kicked client (online/offline) removes vault + toast + home, code rotation verified.
- [ ] Storage: per-client contribution model, ring chart in Settings, vault total = sum of contributions.
- [ ] Demo ops: supervised + caffeinated servers, wipe, 3 seeded vaults per server, VMs up with the app only.
- [ ] Verify (typecheck, build, cargo test/clippy, live check on VMs) and close.

## Checkpoint   (overwrite in place · ≤10 lines · write BEFORE starting the next step)
done:       investigation complete (8 read-only reports); unit specs written for wave 1 (U1a rust profile/names/capacity, U2 ui styling+labels, U3 ts seam+onboarding, U5 backend robustness+profile port, U6 scripts+seeder) and wave 2 (U1b kick hardening, U4 storage ring).
in-flight:  wave 1 implementers running in parallel; each reviewed; fix rounds ≤2.
next:       wave 2 (U1b on node/vault.rs+mod.rs; U4 on StorageTab/events) → full verify → build app + qfsd → wipe → start servers → seed → provision VM1 → VM2 clean → live check.
open:       design chosen: name stored locally (profile.json) + on the directory's side port 8440 (PROFILE_PUT/GET keyed by a machine-derived client id); vault capacity = server allocation + Σ member contributions (8 GiB default per client); server-token holders are vault admins.

## Outcome   (fill at completion · ≤10 lines · facts only)
changed:
verified:
not-done:
gotchas:
